<%= form_with(model: beer_club) do |form| %>
  <% if beer_club.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
        <% beer_club.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control', required: true %>
    <div class="invalid-feedback">
      Please provide a name for the beer club.
    </div>
  </div>

  <div class="mb-3">
    <%= form.label :founded, class: 'form-label' %>
    <%= form.number_field :founded, class: 'form-control', required: true %>
    <div class="invalid-feedback">
      Please provide a founding year.
    </div>
  </div>

  <div class="mb-3">
    <%= form.label :city, class: 'form-label' %>
    <%= form.text_field :city, class: 'form-control', required: true %>
    <div class="invalid-feedback">
      Please provide a city.
    </div>
  </div>


  <div>
    <%= form.submit "Create Beer Club", class: 'btn btn-primary' %>
  </div>
<% end %>
